export declare class Utils {
    static kebabStyle(input: string): string;
    static life(input: string): string;
    static getRanInt(max: number): number;
    static checkArrLen(arrLen: number, currPos: number): number;
}
